the classic mobile bottleneck. While your desktop setup is perfectly optimized, the simulated mid-tier mobile hardware and throttled 4G network connection on Lighthouse Mobile change the game entirely.Your overall mobile score drops to 62/100, driven primarily by a 7.87-second LCP (scoring 3/100) and a 505 ms TBT (scoring 58/100).Because your FCP is fantastic at 912 ms, we know the browser parses your static index.html and downloads the 9.5 KB mobile hero image almost instantly. The massive 7-second gap between FCP (912 ms) and LCP (7,870 ms) points to exactly two hidden mobile bottlenecks:1. The Interaction Trigger vs. PageSpeed BotLook at your script loader code again. It delays your heaviest scripts (GTM, Google Ads cascade, and tracker) until the first user interaction, or via a fallback timer:javascriptsetTimeout(scheduleAllScripts, 15000);
Use code with caution.The Mobile Culprit: The PageSpeed mobile bot never interacts with the screen, meaning it waits out the main thread. However, Lighthouse Mobile applies heavy CPU throttling (usually a 4x or 4.5x slowdown).The Bottleneck: While your main Vite bundle (/assets/index-D4atXg4k.js) executes lightly on desktop, a throttled mobile CPU chokes on parsing and hydrating the React tree. If React execution takes a long time, it locks up the main thread right when the browser wants to complete the styled paint of the hero image. This is backed up by your 505 ms TBT, which is purely JS execution tax.2. Media Query Overlap or Preload CompetitionEven though your markup is perfect, when Lighthouse throttles the network connection down to a simulated 1.6 Mbps mobile profile, asset prioritization becomes highly sensitive.If your CSS preload bundle is large, the throttled mobile network forces the preloaded image and the preloaded CSS to fight for the exact same narrow bandwidth stream.If the CSS bundle takes 3-4 seconds to trickle down over a throttled connection, the browser cannot paint the hero background styling, delaying LCP.How to Narrow This DownCould you tell me:What is the uncompressed network file size of your main JavaScript bundle (index-D4atXg4k.js)?What is the file size of the newly preloaded CSS file?If your JS bundle is quite large, I can show you how to set up Vite code-splitting (manualChunks) to keep the mobile main thread completely clear during initial hydration!