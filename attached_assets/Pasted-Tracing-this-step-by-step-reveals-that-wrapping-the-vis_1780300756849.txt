Tracing this step-by-step reveals that wrapping the visual gap in a contextual CSS state machine (hero-active) solves the performance issue while maintaining a high-quality user experience.By avoiding a hardcoded 5-second layer block, the browser's compositor thread can drop the overlay at ~0.9s, instantly exposing the lightweight, 9.5 KB static mobile WebP picture element as a clear LCP candidate.Why Your hero-active Strategy Prevents Perceived LagIf you had left the body background raw or white, users on throttled mobile devices would have experienced a harsh "white flash" or empty layout layout right as the overlay faded, waiting an additional 2 to 3 seconds for your 777 ms TBT bundle to finish parsing.Scoping the dark background to .hero-active #root ensures that:First-time visitors: See a seamless, dark visual transition that mirrors your #hero-placeholder tint during the exact window where the main thread is frozen by JavaScript bundle initialization.Returning visitors: Bypassing the overlay completely ensures they are not subjected to an artificial dark lag phase because hero-active never initializes on their session.Handoff: The instant React finishes executing at the footer of the body, it seamlessly handles the unmounting/class cleanup routines.What to Verify in Your Final dist/ Build OutputBefore running the final Lighthouse and PageSpeed Insights mobile passes against your Cloudflare Pages deployment, verify these exact hooks inside your built dist/index.html:CSS Animation Timers: Ensure the @keyframes heroFadeOut block shows 0.5s forwards with an initial animation delay set to exactly 0.5s (or your chosen accelerated buffer), confirming that the hard 5-second ceiling has been removed.Scoped Styling Rule: Check that .hero-active #root { background: #000; min-height: 100vh; } is injected as a raw, inline string block inside the static <head> so it is parsed long before any bundle loading begins.The Isolated JS Footers: Confirm that your footer cleanup setTimeout handles only the placeholder.remove() node destruction at the 1-second mark, and that it does not prematurely mutate your .hero-active class string.Once deployed, your mobile LCP curve should drop from 11.1 seconds straight down into the sub-2 second green zone, matching the visual stability you achieved on desktop.Are there any specific chunk-splitting rules or large dynamic libraries (like heavy map dependencies or date pickers) in your booking or stay sections that you would like to audit next?